# Server Config
PORT=3000
API_KEY=your_secret_api_key_here

# Database Config (MySQL) - Optional, fallback to memory logs if connection fails
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=handcap_db

# Session Config
SESSION_DIR=./sessions
